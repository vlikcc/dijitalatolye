apiVersion: v1
kind: Service
metadata:
  name: {{ include "dijitalatolye.fullname" . }}
  labels:
    {{- include "dijitalatolye.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "dijitalatolye.selectorLabels" . | nindent 4 }}
